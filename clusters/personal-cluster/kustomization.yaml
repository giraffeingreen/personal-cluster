apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Aggregate the cluster resources managed by Flux.
# Note: Do NOT set a root-level namespace here to avoid overriding
# the explicit namespaces defined in child manifests (e.g., flux-system).
resources:
  - ./flux-system
  - ./secrets
  - ./notifications