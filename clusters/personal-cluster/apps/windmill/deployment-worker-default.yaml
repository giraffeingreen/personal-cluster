apiVersion: apps/v1
kind: Deployment
metadata:
  name: windmill-worker-default
  namespace: windmill
  labels:
    app: windmill-worker-default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: windmill-worker-default
  template:
    metadata:
      labels:
        app: windmill-worker-default
    spec:
      containers:
        - name: windmill-worker
          image: ghcr.io/windmill-labs/windmill:main@sha256:247320bd5d590ac86fa6ee8961b6692a71dd8331b3f7a1e236f9c0c25338abd7 # {"$imagepolicy": "flux-system:windmill-main"}
          env:
            - name: MODE
              value: worker
            - name: WORKER_GROUP
              value: default
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: timescale-secret
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: timescale-secret
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: timescale-secret
                  key: POSTGRES_DB
            - name: POSTGRES_HOST
              value: timescaledb.timescale.svc.cluster.local
            - name: POSTGRES_PORT
              value: "5432"
            - name: DATABASE_URL
              value: postgresql://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@$(POSTGRES_HOST):$(POSTGRES_PORT)/$(POSTGRES_DB)
          volumeMounts:
            - mountPath: /var/run/docker.sock
              name: docker-socket
            - mountPath: /tmp/windmill/cache
              name: worker-cache
            - mountPath: /tmp/windmill/logs
              name: worker-logs
          resources:
            limits:
              memory: "3072Mi"
              cpu: "2000m"
            requests:
              memory: "1024Mi"
              cpu: "500m"
      volumes:
        - name: docker-socket
          hostPath:
            path: /var/run/docker.sock
        - name: worker-cache
          emptyDir: {}
        - name: worker-logs
          emptyDir: {}
