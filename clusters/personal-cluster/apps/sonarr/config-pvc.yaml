apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-config
  namespace: plex
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: openebs-hostpath
  volumeName: sonarr-config-pv
  resources:
    requests:
      storage: 5Gi
