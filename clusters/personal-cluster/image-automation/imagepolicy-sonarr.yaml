apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: sonarr-latest
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: sonarr
  interval: 168h
  filterTags:
    pattern: "^latest$"
  policy:
    alphabetical:
      order: asc
  digestReflectionPolicy: Always
