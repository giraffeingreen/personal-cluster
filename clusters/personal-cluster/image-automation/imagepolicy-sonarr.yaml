apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: sonarr-latest
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: sonarr
  policy:
    alphabetical:
      order: asc
