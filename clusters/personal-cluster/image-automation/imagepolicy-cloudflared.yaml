apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: cloudflared-latest
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: cloudflared
  filterTags:
    pattern: "^latest$"
  policy:
    alphabetical:
      order: asc
